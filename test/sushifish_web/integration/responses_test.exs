#defmodule ResponsesIntegrationTest do
  #use ExUnit.Case, async: true
  ##use Plug.Test
  #use SushifishWeb.ConnCase
  #alias SushifishWeb.Router

  #@opts Router.init([])
  #test "response codes" do
    #conn = conn(:get, "/")
    #response = Router.call(conn, @opts)

    #assert response.status == 200
  #end
#end
